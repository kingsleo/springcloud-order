spring:
  application:
    name: order
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: test
  main:
    allow-bean-definition-overriding: true
server:
  port: 8081